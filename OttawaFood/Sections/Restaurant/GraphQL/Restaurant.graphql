mutation Restaurant($yelpId: ID!) {
  createOrRetrieveRestaurant(input: {yelpId: $yelpId}) {
    restaurant {
      id
      yelpId
      name
      imageUrl
      yelpUrl
      phone
      yelpRating
      priceLevel
      favorite
      inBucketList
    }
  }
}
